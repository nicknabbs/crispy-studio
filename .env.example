# Anthropic API key used by netlify/functions/pancake-chat.ts (the "Pancake" chatbot).
# Create a key at https://console.anthropic.com/ and set it here (and in Netlify env vars for prod).
ANTHROPIC_API_KEY=sk-ant-...
